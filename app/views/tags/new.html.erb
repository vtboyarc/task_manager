<div id="header"><h1>Create a New Tag</h1></div>
<div id="newsub">

<%= form_for @tag do |t| %>

  <p>
    <%= t.label :name %>
    <%= t.text_field :name %>
  </p>
	
	<%= t.submit value: "Create Tag"%>

<% end %>

</div>
<p><%= link_to("Cancel New", tags_path)%>