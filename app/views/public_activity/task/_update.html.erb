			
			<%if activity.trackable.completed == true %>
					 's task "<%= activity.trackable.name %>"  was completed! 
			<%else%>
				's task "<%= activity.trackable.name %>" was updated 
			<%end%>
		
				 