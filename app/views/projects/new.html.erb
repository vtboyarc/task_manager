PROJECTS NEW

<%= form_for @project do |p| %>

  <p>
    <%= p.label :name %>
    <%= p.text_field :name %>
  </p>
	<p>
    <%= p.label :desc %>
    <%= p.text_field :desc %>
  </p>
	
	<%= p.submit %>

<% end %>